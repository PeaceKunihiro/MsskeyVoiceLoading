# 更新履歴

## v0.2.0 - 2026-08-25

### VOICEVOX対応

- 棒読みちゃんとWebSocket Pluginへの依存を廃止しました。
- VOICEVOX ENGINEの `/audio_query` と `/synthesis` を使った音声生成に変更しました。
- Manifest V3のoffscreen documentによるWAV再生を追加しました。
- `/speakers` からキャラクターとスタイルを取得できるようにしました。
- 話速、音高、抑揚、音量、ENGINE URLの設定を追加しました。
- 別PCのENGINEには、指定した接続先だけの任意ホスト権限を要求するようにしました。
- VOICEVOX未起動時のエラー処理とテスト読み上げを追加しました。

### 読み上げ制御

- ツールバーアイコンによる読み上げON/OFF切り替えを追加しました。
- ON/OFF状態を設定画面と `chrome.storage.sync` で同期しました。
- バッジでON/OFF状態を表示するようにしました。
- OFF操作時に再生中音声を停止し、待機キューを破棄するようにしました。
- OFF中のノートを既読扱いにし、ON復帰後に遡って読まないようにしました。
- MisskeyユーザーIDごとにVOICEVOXの話者・スタイルを割り当てる「カスタム読み上げ」を追加しました。
- ローカルID（`@user`）と完全ID（`@user@host`）を保存できるようにしました。
- カスタム話者が未登録・不正・削除済みの場合はデフォルト話者へフォールバックするようにしました。
- 複数のstyle IDからノートごとに選択するランダム声音を追加しました。
- ランダム対象styleごとの `volumeScale` 設定を追加しました。
- Web Audio APIの `StereoPannerNode` によるstyle別ステレオバランスを追加しました。
- 同時読み上げ数（1～4）と、VOICEVOX生成・再生のワーカープールを追加しました。
- 並行再生ごとに独立した `StereoPannerNode` を使用するようにしました。
- 読み上げ対象Misskeyインスタンスを複数登録できるようにしました。
- インスタンスURLをorigin単位で正規化し、任意ホスト権限と動的content scriptで管理するようにしました。
- インスタンス削除時に、開いている該当タブの読み上げを停止するようにしました。
- Misskeyホスト権限を要求する前に、取得情報と用途を示す独自確認ダイアログを追加しました。

### 初期ノートと本文整形

- ロード済ノートの初回読み上げ件数を設定可能にしました（初期値5件）。
- DOM追加順ではなく実投稿日時で最新N件を選び、古い順に読み上げるようにしました。
- `initialNoteCount = 0` の場合は既存ノートを読まず、初期化時刻を通常監視の基準にするようにしました。
- 最大読み上げ文字数を追加しました（初期値64文字）。
- 本文整形後、Unicodeコードポイント単位で文字数を制限するようにしました。
- 本文中のHTTP/HTTPS URLだけを除去する設定を追加しました（初期値ON）。
- URL除去後の余分な空白を整理するようにしました。
- バックグラウンドタブでも新着DOMを処理できるよう、`requestAnimationFrame` 依存を撤去しました。

### 投稿時刻と読み上げキュー

- ノート自身の `time` 要素から投稿日時を取得するようにしました。
- `datetime`、`title`、親要素の `title`、`data-timestamp`、`data-time`、`aria-label` に対応しました。
- ISO日時、Unix秒・ミリ秒、日本語絶対日時を解析できるようにしました。
- 日時取得失敗時に現在時刻を代入しないよう修正しました。
- `latestAcceptedTimestamp` より古い遅延描画ノートを除外するようにしました。
- 同一時刻のノートはNote IDによる重複判定を併用して個別に処理するようにしました。
- 待機キューを投稿日時順で管理するようにしました。
- 同時追加されたノートを短時間集約してから時刻順に処理するようにしました。
- 待機キューの最大件数を追加しました（初期値10件）。超過時は待機中の最古ノートを破棄します。
- 投稿の有効時間を追加しました（初期値300秒、0で無制限）。
- 鮮度をキュー投入時と再生直前の2回判定するようにしました。
- 有効時間が設定されている場合、日時不明のノートを除外するようにしました。
- 時刻属性、解析結果、経過秒数、設定値、判定結果のデバッグログを追加しました。

### DOM監視

- `MutationObserver` による新着ノート監視を実装しました。
- `data-scroll-anchor` とNote IDによる重複防止を実装しました。
- CW、引用・返信元、通常Renote、画像・ファイルのみの投稿を除外しました。
- カスタム絵文字を読み上げ本文から除外しました。
- 中央タイムライン以外の通知・ウィジェットを除外しました。
- Mutation候補を150ms集約し、Note IDで重複排除してから最新候補だけを処理するようにしました。
- `getBoundingClientRect()` によるviewport交差判定を追加しました。
- 上限超過・画面外・遅延描画候補を既読扱いで破棄するようにしました。
- タブ復帰時に背景中の古い候補を遡って読まないようにしました。

## v0.1.1 - 2026-08-24

- 棒読みちゃん標準TCPポート50001へのWebSocket接続を拒否しました。
- WebSocket PluginのJSON形式を修正しました。
- 接続テスト、タイムアウト、再接続処理を追加しました。

## v0.1.0 - 2026-08-24

- Manifest V3形式の試作版を作成しました。
- MisskeyのDOM監視、本文抽出、重複防止、設定画面を実装しました。
- 棒読みちゃんWebSocket Pluginへの送信を試作しました。
